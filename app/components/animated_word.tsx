import { motion } from "framer-motion";

export default function AnimatedWord(props: { word: string }) {

    const { word } = props;

    const splitWord = word.split('');

    return (
        <motion.div>

        </motion.div>
    )


}